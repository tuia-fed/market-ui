export interface RingAnimateState {
  isShow: boolean;
  isInitMove: boolean;
  isStop: boolean;
  id: string;
}

export interface CatchRingAnimateState {
  catchActive: boolean;
  escapeActive: boolean;
}
